{
  "name": "tebako-versions",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.18.0"
  },
  "scripts": {
    "dev": "astro dev",
    "collect": "node tools/collect.ts",
    "prebuild": "npm run collect",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "test": "node --test tools/lib/*.test.ts src/lib/*.test.ts"
  },
  "dependencies": {
    "astro": "7.3.1"
  },
  "allowScripts": {
    "esbuild": true
  },
  "devDependencies": {
    "@astrojs/rss": "4.0.19",
    "yaml": "2.9.0"
  }
}
